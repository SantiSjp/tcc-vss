- Usando esse algoritmo deu para pegar bem os contornos do campo com a imagem vss_field_01.png:
{
    cv::cvtColor(image, image, cv::COLOR_BGR2GRAY);
    cv::threshold(image, image, 128, 255, cv::THRESH_BINARY);
    
    std::vector<std::vector<cv::Point>> contours;
    cv::Mat contourOutput = image.clone();
    cv::findContours(contourOutput, contours, cv::RETR_CCOMP, cv::CHAIN_APPROX_NONE);
}